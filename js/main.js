// PR-001: App bootstrap placeholder.
// Future PRs will initialize DataManager, load medals.json, and render UI.

(function bootstrap() {
  // No-op for now. This ensures the file is a valid script.
})();
